# 改造内容清单（真实后端数据优先版）

日期：2026-03-23
目标：在不破坏现有可编译能力的前提下，确保模拟数据与后端真实报文一致，且模拟与线上完全复用同一业务链路，避免演示专用逻辑。

## 总体原则（硬约束）

1. 模拟数据必须使用后端真实消息结构
- 不允许仅构造 UI 需要的局部字段。
- 必须包含与线上一致的顶层字段（如 type、session_id、data）和业务字段。

2. 模拟入口必须走正式解析与分发链路
- 不允许按钮直接调用 UI 渲染函数。
- 必须经过与线上相同的消息解析、类型分发、业务处理流程。

3. Release 不暴露演示能力
- 调试入口、调试文案、调试按钮只允许在 Debug 可见。
- Release 构建中不出现演示专用交互。

## P0（必须先做）

1. 统一事件入口（提醒/短信状态）
- 目标：线上 WebSocket 与本地注入走同一处理函数，不分叉业务逻辑。
- 改造点：
  - MainActivity：抽出提醒与短信状态统一处理函数。
  - WebSocket 回调仅做数据转发，不做 UI 逻辑。
- 验收：
  - 同一份后端报文，线上接入与本地回放结果一致。
  - 不出现调试专属 UI 处理分支。

2. 调试注入源收敛（Debug 可用，Release 不暴露）
- 目标：演示注入能力保留，但仅在 Debug 生效，并严格使用后端原始报文格式。
- 改造点：
  - 将 mock 入口统一收敛到调试注入层（命名可自定）。
  - 调试注入层输入为原始 JSON 字符串，输出直接复用正式消息分发。
  - activity_main.xml 中 debug 按钮区域仅在 Debug 构建启用（或运行时隐藏）。
- 验收：
  - Debug 可触发提醒/短信成功/短信失败。
  - 三类调试消息均为后端等价报文（非局部字段拼接）。
  - Release 不显示调试入口，不暴露调试文案。

3. 勾叉确认卡正式化（替代临时“文字问答”）
- 目标：把“是否现在服药”做成明确确认组件，支持语音和 TP 手势双通道。
- 改造点：
  - 新增确认卡组件（例如 `ReminderConfirmCardView`）与布局。
  - 提醒流程顺序固定：
    1) 原药品信息卡
    2) 亲和询问语音
    3) 勾叉确认卡
  - 结果事件统一回传主流程（确认/稍后）。
- 验收：
  - 录像中可稳定呈现上述顺序。
  - 语音“确认/稍后”与 TP 操作得到相同结果。

## P1（应完成）

4. TP 焦点与手势映射接入确认卡
- 目标：与《TP事件接入方案_X2》一致，避免临时映射。
- 改造点：
  - 在 MainActivity 的 TP 分发里加“确认卡激活态”分支。
  - 推荐映射：
    - SlideForward / SlideBackward：勾/叉焦点切换
    - Click：确认当前焦点
    - LongClick：取消或关闭确认卡
- 验收：
  - 仅在确认卡可见时拦截上述手势。
  - 关闭确认卡后 TP 行为恢复原业务映射。

5. 消息时序与重复保护
- 目标：防止后端重发/网络抖动导致重复弹卡和重复播报。
- 改造点：
  - 基于 reminder_id + 时间窗去重。
  - 确认卡存在期间，重复提醒只刷新内容或忽略（按产品规则二选一）。
- 验收：
  - 连续收到同 reminder_id 不重复堆叠卡片。
  - TTS 不重复轰炸。

6. 通知权限与降级文案闭环
- 目标：Android 13+ 通知权限拒绝时有明确降级体验。
- 改造点：
  - 权限拒绝后保留 AR Toast + TTS 路径。
  - 在设置页引导文案中说明如何重新开启通知（如已有设置入口则复用）。
- 验收：
  - 用户拒绝权限后，核心提醒流程仍完整可用。

## P2（可选优化）

7. 视觉与文案统一
- 目标：提醒卡、确认卡、短信状态提示的语气与视觉风格一致。
- 改造点：
  - 统一按钮文案、标题语气、图标语义。
  - 统一展示时长与消失动画节奏。
- 验收：
  - 演示中不存在突兀“调试感”文案或样式。

8. 最小埋点（仅关键路径）
- 目标：为拍摄与回归提供证据，但不过度埋点。
- 改造点：
  - 仅记录：提醒触发、确认结果、短信发送结果、权限状态。
  - Debug 打全量，Release 仅保留必要事件。
- 验收：
  - 日志足够复盘一次完整演示链路。

## 建议实施顺序
1. 统一事件入口
2. 调试注入收敛（Debug/Release 隔离）
3. 勾叉确认卡组件
4. TP 手势接入确认卡
5. 去重与时序保护
6. 权限降级闭环
7. 视觉文案统一 + 最小埋点

## 每步完成定义（DoD）
- 功能：主链路可跑通（线上消息与本地回放一致）。
- 数据：调试样本可直接替换为后端抓包原文，无需业务改码。
- 工程：assembleDebug 通过。
- 体验：拍摄顺序稳定为“药品卡 -> 亲和询问 -> 勾叉确认卡”。
- 交付：Release 不暴露调试入口与调试文案。

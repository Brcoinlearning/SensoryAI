package com.narc.arclient.process.processor;

import com.narc.arclient.entity.RecognizeTask;

// ✅ 这是一个空壳，为了防止旧代码报错
public class PreHandleProcessor {

    public RecognizeTask process(RecognizeTask task) {
        // 什么都不做，直接返回
        return task;
    }
}
# UI 计划「主人手册」：给我读的一份就够（2026-03-13）

> 你只需要读这份文档。
> 其它 UI_* 文档是“给 AI 执行用”的说明书；这份是“给你掌控全局用”的导航。

重要：

- **复制/粘贴给 AI 的内容，只允许来自本文件**（第 5 节各阶段的提示词块）。
- 其它文件一律当作“AI 在仓库里自行打开阅读的参考资料”，不再由你手动摘抄。

---

## 1) 这件事到底要达成什么（用一句话说清楚）

在 **不违反 AR 约束**（遮挡小、复杂背景可读、语义色一致、动效克制）前提下，让实机 UI **更精美、更像成品、动效更自然**，并且后续迭代有“可持续的 token/验收/回归”机制。

### 当前口径（你只记 4 条）

1. **只验收 X2**（其它设备先不做强验收）。
2. **优先做“精美 + 成品感 + 动效节奏”**。
3. **黑块伪影（P0-1 / 1A）按需执行**：X2 真机不复现就先跳过。
4. **发热/性能优化后置到阶段 4**，但 **阶段 3 动效必须遵守护栏**（不能为了动效引入常驻负载）。

---

## 2) 你怎么用 AI：一句话工作流

你每次只做三件事：

1) **选一个阶段**（只让一个 AI 做一个阶段）
2) **把“本阶段需要读的材料片段”丢给 AI**（下面每个阶段都写了“最小投喂包”）
3) **只验收交付物**（交付摘要 + 修改文件 + 验收截图/命令 + 风险点）

> 核心原则：**不要让 AI 同时做两件大事**；你只要会“选阶段 + 验收”，就能掌控进度。

### 你真正关心的规则（解决“到底粘贴哪份文件”）

从现在开始只执行这一条硬规则：

- **你只从本文件复制粘贴**（每个阶段下面都有“可直接复制给 AI 的提示词块”）。
- 其它文档（Master/Onboarding/QuickWins/Workflow/AI-0）都**不需要你手动摘抄粘贴**：在提示词块里会要求 AI 自己去打开这些文件阅读。

这样你永远不会陷入“我该贴 Owner 还是 Master 还是 Workflow”的选择题。

---

## 3) 文件怎么分工：你读什么、AI 读什么

### 你只需要读

- 本文件：`UI_Owner_Guide_2026-03-13.md`

### 你需要知道的“参考资料”（让 AI 自己打开读，不要你摘抄）

1) **总路线图（工程事实 + 阶段框架）**：UI_Master_Roadmap_2026-03-12.md
- 你看它的目的：理解阶段切分、工程事实（Render vs View）、交付模板。

2) **交接/验收框架（每阶段要产出什么）**：UI_Optimization_Plan_Agent_Onboarding_2026-03-12.md
- 你看它的目的：知道“阶段任务 + 验收标准 + 任务卡”。

3) **快速收益清单（最小改动最大观感）**：UI_Quick_Wins_Execution_Plan_2026-03-12.md
- 你看它的目的：当你想“先快速变好看”，直接按 P0/P1 执行。

4) **AI-0 工程结论快照（架构/决策树/覆盖清单/任务卡）**：AI-0_UI_Roadmap_Fill_2026-03-12.md

备注：历史的提示词库 AI_UI_Workflow_Prompts_2026-03-12.md 已不再作为复制入口（提示词已迁移到本文件）。

> 其它：UI_Design_Review_Report_2026-03-12.md + 实机图片/ 是“证据基线”。

---

## 4) 你现在在进度条的哪里？（以及下一步最推荐做什么）

### 当前状态（截至 2026-03-13）

- ✅ 文档口径已统一：X2-only、1A 按需、动效护栏、性能后置到阶段 4、任务卡粒度。
- ✅ AI-0 交付已按交接框架补齐，可作为后续执行入口：AI-0_UI_Roadmap_Fill_2026-03-12.md
- ⏳ 真实工程落地尚未开始（代码/资源未改）。

### 最推荐的下一步

- **先做阶段 1B（P0-2：按钮配色收口）**：这是“观感立刻成体系”的最短路径。
- 同时准备阶段 3（质感 + 动效）：但要先产出“动效清单与护栏表”，再落地。

---

## 5) 每个阶段：你要喂 AI 哪些“片段”，目标是什么

下面每个阶段都给了：
- 你要做的事（你只负责选择和验收）
- 1 段“可直接复制给 AI 的提示词块”（你只复制这一段）
- AI 交付目标（你验收它有没有给到）

> 重要：你不确定时默认“只读”，禁止 AI 改任何文件；让它先交付清单/方案/验收方式。

### 读 / 写分界线（避免 AI 误改工程）

- 只想“帮你理解/梳理/出清单”：只使用提示词库里的 **阶段 0/1/2/3**（这些阶段要求 *只读* 或 *只输出方案*）。
- 只有当你明确说“**允许修改代码**”时，才进入提示词库的 **阶段 4（允许改代码）**；并且要写清楚“只改哪个 P0 项”。
- 你不确定要不要改时：默认 **禁止修改任何文件**，先让 AI 产出“改动清单 + 文件定位 + 验收方式”。

---

### 阶段 0：基线与回归闭环（只做一次）

完成情况（2026-03-13，X2）：已产出两张表（问题-证据-优先级表 + 发版必跑验收 checklist），后续发版可直接使用该文档作为验收入口：
- [Stage0_X2_验收输出_问题证据优先级_必跑Checklist_2026-03-13.md](Stage0_X2_验收输出_问题证据优先级_必跑Checklist_2026-03-13.md)

**你要做的事**：让 AI 建立“验收基线”与 checklist，后面每次改动都能对照。

**可直接复制给 AI 的提示词块**：

```text
你是执行 AI（阶段 0：基线与回归闭环）。请只做“只读分析”，不要修改任何文件、不要改代码。

口径：只验收 X2；P0-1 黑块伪影按需；优先做“精美+成品感+动效节奏”；动效禁止无意义常驻循环。

你需要在仓库内自行打开并阅读这些材料（不要让我复制粘贴内容给你）：
- UI_Design_Review_Report_2026-03-12.md
- UI_Optimization_Plan_Agent_Onboarding_2026-03-12.md（项目口径/总体验收原则/阶段0）
- 实机图片/（四张 baseline：实时字幕、拍照追问1、拍照追问2、药品识别）

输出要求（必须是两张表格）：
1) 问题-证据-优先级表：每条问题必须引用至少 1 张截图文件名作为证据。
2) 发版必跑验收 checklist：列（验收项/场景/如何拍 After 图/Pass 标准/Baseline）。要求 10–15 分钟能跑完。

限制：不要给长篇建议；先把表格写到可直接照做。
```

**AI 交付目标**：

- 1 个“问题-证据-优先级表”（引用截图文件名）
- 1 份“发版必跑验收 checklist”（颜色语义/遮挡/可读性/伪影/边缘裁切/并发信息竞争）

---

### 阶段 1A（P0-1）：黑块伪影（按需）

**你要做的事**：先判断是否要做。

- 若 X2 真机 **能复现**：做 1A。
- 若 **不能复现**：跳过 1A，直接做 1B。

**可直接复制给 AI 的提示词块**：

```text
你是执行 AI（阶段 1A：黑块伪影，按需）。请先做“只读定位方案”，不要修改任何文件、不要改代码。

前置判断：如果 X2 真机无法复现黑块伪影，你要在结论里明确建议“跳过 1A，先做 1B+3”。

你需要在仓库内自行打开并阅读这些材料：
- AI-0_UI_Roadmap_Fill_2026-03-12.md（B：伪影决策树、A：层级推论）
- UI_Quick_Wins_Execution_Plan_2026-03-12.md（P0-1 章节）
- 实机图片/拍照追问1.png、实机图片/拍照追问2.png

输出格式（必须按这个结构）：
1) 复现路径（步骤+条件；如果不可复现写“不可复现”并说明你试图怎么复现）
2) 归因切分计划（Render vs View）：用最小验证证明来源
3) 决策树（≥6 分支）：每分支=验证动作/建议的临时开关/预期现象/结论
4) 如果需要改代码：先只给“最小改动清单+改哪些文件+验收怎么拍 After 图”，等我确认后再进入允许改代码
```

**AI 交付目标**：

- 复现路径（步骤 + 条件）
- 定位决策树（至少 6 个分支）
- 最小修复方案（修改哪些文件、为什么）
- 验收：Before/After 截图对照（同场景）

---

### 阶段 1B（P0-2）：按钮配色收口（优先做）

**你要做的事**：让 AI 把“紫/橙/洋红等割裂色”收回到 token 体系，形成统一的青绿视觉语言。

**可直接复制给 AI 的提示词块**：

```text
你是执行 AI（阶段 1B：按钮配色收口，Render 层优先）。先产出“方案与改动清单”，不要直接改代码；等我确认后再进入允许改代码。

口径：只验收 X2；目标是统一到青绿体系；红色只用于错误/危险/停止语义；After 不再出现紫/橙/洋红/黄等割裂色。

你需要在仓库内自行打开并阅读这些材料：
- AI-0_UI_Roadmap_Fill_2026-03-12.md（C：配色覆盖面清单、D：1B 任务卡）
- DESIGN_SYSTEM.md
- app/src/main/res/values/design_system.xml
- app/src/main/java/com/narc/arclient/process/processor/RenderProcessor.java
- 实机图片/拍照追问1.png、实机图片/药品识别.png（用于验收对照）

输出格式（必须按这个结构）：
1) 语义色映射表（默认/聚焦/按下/禁用/成功/警告/错误/处理中/聆听）：每项指向具体 token 名称（或建议新增的 token 名称）。
2) 改动清单：列出要替换的硬编码色与目标 token（按 Render/View 分组）。
3) 修改文件列表（预期将改哪些文件）。
4) 如何验收：需要拍哪些 After 图、以及“After 应该看到哪些颜色消失”。
```

**AI 交付目标**：

- 语义色映射表（默认/聚焦/按下/禁用/成功/警告/错误/处理中/聆听）
- 修改文件列表（重点是 RenderProcessor + token）
- 验收口径：After 不再出现紫/橙按钮底色；红色只用于错误/危险语义

---

### 阶段 2：遮挡与信息密度（后做，但很关键）

**你要做的事**：让长内容不遮挡视野中心；卡片与字幕同屏要有规则。

**可直接复制给 AI 的提示词块**：

```text
你是执行 AI（阶段 2：遮挡与信息密度）。请只输出“结构方案 + 验收方式”，不要修改任何文件、不要改代码。

你需要在仓库内自行打开并阅读：
- UI_Optimization_Plan_Agent_Onboarding_2026-03-12.md（阶段2）
- 实机图片/拍照追问2.png（baseline）
- app/src/main/res/layout/item_ar_card.xml
- app/src/main/java/com/narc/arclient/MainActivity.java（卡片内容/位置策略相关部分）

输出要求：
1) 信息结构方案：结论先行 + 要点列表 + 分页/继续查看（优先分页）。
2) 最大高度与同屏规则：卡片与字幕同屏时谁降权、持续多久。
3) 验收：同场景 After 截图清单（具体到文件名）。
```

**AI 交付目标**：

- 信息结构改造方案（结论先行 + 要点 + 分页/继续查看）
- 最大高度策略 + 同屏规则
- 验收：同场景 After 截图清单

---

### 阶段 3：质感精修 + 动效节奏（你关心的“更漂亮”主要在这）

**你要做的事**：让 UI 看起来更“系统级”，但不引入常驻耗电。

**可直接复制给 AI 的提示词块**：

```text
你是执行 AI（阶段 3：质感精修 + 动效节奏）。先交付“动效清单与护栏表”，不要直接改代码；等我确认后再给出实现改动清单。

动效护栏：除 Listening/Processing 外禁止无限循环；任何循环必须有停止条件；避免为了动效引入持续重绘/常驻刷新。

你需要在仓库内自行打开并阅读：
- UI_Quick_Wins_Execution_Plan_2026-03-12.md（P1-1/P1-3/P1-4 + 动效护栏）
- DESIGN_SYSTEM.md
- app/src/main/res/drawable/bg_card_premium.xml、bg_subtitle_premium.xml、bg_status_bar.xml
- app/src/main/res/anim/

输出要求：
1) 动效清单与护栏表：组件→触发→时长(token)→停止条件→验收怎么拍。
2) 质感统一点清单：文字对比/容器边框/分割线一致性（只列可执行项）。
3) 验收：亮背景+复杂背景各至少 1 张 After 图要求。
```

**AI 交付目标**：

- 先交付“动效清单与护栏表”（组件→触发→时长 token→停止条件）
- 再交付动效实现改动（卡片淡入淡出 + 小幅 translate/scale；Toast 小幅 slide+alpha）
- 验收：亮背景/复杂背景下依旧清晰；动效不抢戏

---

### 阶段 4：可持续化（token/QA/发热性能）

**你要做的事**：让工程以后不靠“感觉”做 UI——靠 token、表格与回归脚本。

**可直接复制给 AI 的提示词块**：

```text
你是执行 AI（阶段 4：可持续化）。请只产出文档表格，不要修改任何工程代码。

你需要在仓库内自行打开并阅读：
- UI_Optimization_Plan_Agent_Onboarding_2026-03-12.md（阶段4 + 发热/性能优化段）
- app/src/main/res/values/design_system.xml
- app/src/main/res/values/styles.xml
- app/src/main/java/com/narc/arclient/process/processor/RenderProcessor.java

输出要求（必须是 3 个表格）：
1) token → 组件映射表（token/语义/组件/状态/引用位置）。
2) 发版必跑 QA 清单（验收项/场景/如何拍图/Pass/Baseline）。
3) 10–15 分钟真机回归记录模板（发热/卡顿主观 + 可选客观指标 1 项）。
```

**AI 交付目标**：

- token → 组件映射表（表格）
- 发版必跑 QA 清单（表格）
- 10–15 分钟真机回归记录模板（发热/卡顿主观 + 可选客观指标 1 项）

---

## 6) 你如何验收 AI 是否“交付到位”（你只看这个）

每个阶段的 AI 交付必须包含以下 5 项，不满足就让它补：

1. 完成了什么（3–7 条要点）
2. 没做什么（明确排除项）
3. 修改的文件列表
4. 如何验收（对照 baseline 截图 + 至少 1 条可跑命令）
5. 风险点（1–3 条）

> 这个模板在 UI_Master_Roadmap_2026-03-12.md 里也有（`最短交接口径`）。

---

## 7) 如果你“对工程没感觉”，怎么快速建立感觉（30 分钟）

你只做两步：

1) 打开 UI_Master_Roadmap_2026-03-12.md，只读 `关键工程事实`（Render vs View）
2) 复制本文件第 5 节里你需要的“阶段提示词块”，让 AI 做一次只读扫描

你最终只需要得到一句话结论：
- “哪些 UI 是 XML View（能用 ripple/anim）”
- “哪些是 RenderProcessor Canvas（要在绘制逻辑里做反馈）”

---

## 8) 最小化建议：以后只保留哪几份文档？（可选）

如果你想减少文档数量（不影响 AI 执行），建议最终只保留：

- UI_Owner_Guide_2026-03-13.md（给你读）
- UI_Master_Roadmap_2026-03-12.md（总控路线图 + 工程事实）
- UI_Optimization_Plan_Agent_Onboarding_2026-03-12.md（任务与验收框架）
- UI_Quick_Wins_Execution_Plan_2026-03-12.md（快速收益清单）

其它材料（评审报告、提示词库）作为附件即可。

# 后端连接方式验证（2026-03-16）

本文档用于记录当前项目与后端服务的实际联通方式验证结果。

说明：
- 本文档是项目侧补充说明。
- 不修改 [能力介绍 & API.txt](能力介绍%20&%20API.txt)，因为该文件属于官方文档，可能不适合混入项目联调结论。

## 1. 当前验证结论

当前 ngrok 地址：

`https://emotionless-kneadingly-tora.ngrok-free.dev`

健康检查：

```bash
curl https://emotionless-kneadingly-tora.ngrok-free.dev/health
```

返回：

```json
{"status":"ok"}
```

说明后端服务在线。

## 2. 正确的 WebSocket 连接方式

当前已实测可成功建立 WebSocket 握手的地址：

1. 音频流

```text
wss://emotionless-kneadingly-tora.ngrok-free.dev/ws/audio_stream
```

2. 图片流

```text
wss://emotionless-kneadingly-tora.ngrok-free.dev/ws/image_stream
```

3. 带 section_id 的音频流

```text
wss://emotionless-kneadingly-tora.ngrok-free.dev/ws/audio_stream?section_id=test-section
```

4. 带 section_id 的图片流

```text
wss://emotionless-kneadingly-tora.ngrok-free.dev/ws/image_stream?section_id=test-section
```

验证结果：

- 握手成功
- 连接成功建立
- 连接后 2 秒内未收到服务端主动消息
- 该行为符合“服务端等待客户端主动发送音频/图片帧”的流式接口特征

## 3. 正确测试方法

不要使用普通 HTTP 请求库去代替 WebSocket 测试。

错误示例：

```python
requests.get(...)
```

原因：

- `requests.get` 不能完成 WebSocket upgrade 后的双向帧通信
- 即使能发起普通 HTTP 请求，也不能代表 WebSocket 流式链路可用

正确示例：

```python
import asyncio
import websockets

async def test(url):
    async with websockets.connect(url, proxy=None, open_timeout=10, close_timeout=5) as ws:
        print("CONNECTED", url)
        try:
            msg = await asyncio.wait_for(ws.recv(), timeout=2)
            print("RECV", repr(msg))
        except asyncio.TimeoutError:
            print("NO_IMMEDIATE_MESSAGE")

async def main():
    await test("wss://emotionless-kneadingly-tora.ngrok-free.dev/ws/audio_stream")
    await test("wss://emotionless-kneadingly-tora.ngrok-free.dev/ws/image_stream")

asyncio.run(main())
```

本次项目中实际验证通过的关键点：

- 使用 `websockets.connect(...)`
- 显式设置 `proxy=None`
- 在 shell 中去掉代理环境变量影响

建议测试命令：

```bash
env -u http_proxy -u https_proxy -u HTTP_PROXY -u HTTPS_PROXY -u ALL_PROXY -u all_proxy -u NO_PROXY -u no_proxy python3 - <<'PY'
import asyncio
import websockets

async def test(url):
    print("\nTEST", url)
    try:
        async with websockets.connect(url, proxy=None, open_timeout=10, close_timeout=5) as ws:
            print("CONNECTED")
            try:
                msg = await asyncio.wait_for(ws.recv(), timeout=2)
                print("RECV", repr(msg)[:300])
            except asyncio.TimeoutError:
                print("NO_IMMEDIATE_MESSAGE")
    except Exception as e:
        print("ERROR", type(e).__name__, str(e))

async def main():
    await test("wss://emotionless-kneadingly-tora.ngrok-free.dev/ws/audio_stream?section_id=test-section")
    await test("wss://emotionless-kneadingly-tora.ngrok-free.dev/ws/image_stream?section_id=test-section")

asyncio.run(main())
PY
```

## 4. 与项目代码对应关系

当前项目中的 WebSocket 路径定义在：

- [app/src/main/java/com/narc/arclient/network/WebSocketManager.java](app/src/main/java/com/narc/arclient/network/WebSocketManager.java)

当前关键常量：

```java
private static final String BASE_URL = "wss://emotionless-kneadingly-tora.ngrok-free.dev";
private static final String AUDIO_STREAM_PATH = "/ws/audio_stream";
private static final String IMAGE_STREAM_PATH = "/ws/image_stream";
```

说明：

- 这组路径与本次真实 WebSocket 测试结果一致
- 因此“路径写错”目前不是首要怀疑点

## 5. 当前更可能的问题位置

既然路径和握手本身已经验证通过，那么应用内仍出现 `websocket error` 时，更可能在以下环节：

1. Android 设备运行时网络环境与本机终端环境不同
2. `OkHttp WebSocket` 连接建立后，发送首帧数据时被服务端关闭
3. 音频流或图片流的数据格式与服务端协议不匹配
4. `section_id`、`session_id` 或消息结束帧不符合服务端预期
5. 客户端错误日志过于粗，未显示响应码、关闭码和具体异常

## 6. 后续排查建议

如果后续继续联调，建议按这个顺序查：

1. 在 Android 端增强 `WebSocketManager.onFailure()` 日志，打印异常、响应码、响应头、关闭原因
2. 分别验证“仅连接不发数据”和“连接后发送第一帧数据”两个阶段
3. 单独验证音频流和图片流，不要同时改两个通道
4. 如果服务端要求首帧必须是 JSON 控制消息，需要补协议握手帧，而不是直接发裸二进制

## 7. 本次结论摘要

本次以真实 WebSocket 客户端完成验证后，可以确认：

- 当前后端地址可用
- `wss://.../ws/audio_stream` 可连接
- `wss://.../ws/image_stream` 可连接
- 带 `section_id` 的连接同样可连接
- 正确的测试方式是 `websockets.connect(...)`，不是 `requests.get(...)`

因此，后续排查重点应从“路径是否错误”转向“应用内发送/协议细节是否匹配”。
# Stage 1B 配色收口：颜色选择规范（最小可用集合）

更新时间：2026-03-13  
适用范围：X2 当前版本 UI（Render 层 + View 层）

## 1. 文档目标

给“改颜色代码的执行 AI”一份可直接落地的选择规范，确保：

1. 优先复用现有 token，不引入随意色值。
2. 同一个显示语境内，颜色与语义一一对应，不重复抢语义。
3. 避免“全换成同一个主题色”。

## 2. 最小可用颜色集合（结论）

### 2.1 色相家族（hue families）

总计 4 套：

1. 主交互青绿家族：默认、聚焦、按下、悬停进度、处理中进度、文本强调。
2. 成功家族：完成/成功结果。
3. 危险家族：错误/危险/停止。
4. 中性结构家族：禁用、描边、分割线、阴影/遮罩。

### 2.2 token 数量

总计 14 个（全部复用现有 token，无新增）。

| token | HEX | 主用途 |
|---|---|---|
| primary_teal | #00A896 | 默认主交互色 |
| primary_teal_light | #02C39A | 聚焦/处理中强调 |
| primary_mint | #00F5D0 | 悬停进度/高亮 |
| interaction_pressed_teal | #00897B | 按下态 |
| interaction_disabled | #6B7A8F | 禁用态 |
| status_success | #05CE78 | 成功 |
| status_warning_cool | #00B894 | 警告 |
| status_error | #D62828 | 错误/危险/停止 |
| text_accent | #00F5D0 | 文本强调 |
| border_subtle | #1A415A77 | 轻边框 |
| border_emphasis | #4D415A77 | 强边框 |
| divider_line | #26E0E1DD | 分割线 |
| overlay_medium | #80000000 | 阴影/文字阴影 |
| overlay_dim | #B3000000 | 遮罩压暗 |

## 3. 角色 -> token 选择表（执行主表）

| 角色 | 允许 token | 组件范围 | 触发条件 | 禁止使用场景 |
|---|---|---|---|---|
| 默认 | primary_teal | 按钮、状态图标默认态 | idle、可交互未聚焦 | 错误/禁用 |
| 聚焦 | primary_teal_light | 光标、按钮焦点环、状态 icon | hover/focus/listening/processing | 错误/停止 |
| 按下 | interaction_pressed_teal | 按钮按下反馈 | press down 到 release | 常驻文本/边框 |
| 禁用 | interaction_disabled | 禁用按钮、光标冻结态 | locked/不可触发 | 成功/警告/错误 |
| 悬停进度 | primary_mint | 按钮环形进度 | hoverProgress > 0 | 关闭读条/错误 |
| 处理中进度 | primary_teal_light | 光标识别进度、处理中 icon | progress > 0 或 processing | 危险/停止 |
| 成功 | status_success | 卡片标题、成功反馈 | completed/识别成功 | 默认主按钮 |
| 警告 | status_warning_cool | 状态条、提示条、卡片提示 | 风险提醒、可恢复异常 | 致命错误/停止 |
| 错误/危险 | status_error | 停止块、关闭读条、失败卡片 | fail/danger/stop | 聆听中/处理中/默认态 |
| 文本强调 | text_accent | 卡片标题、关键字段 | 结论先行、关键词强调 | 长段正文 |
| 描边/分割线 | border_subtle / border_emphasis / divider_line | 卡片、字幕、状态条容器 | 结构层次常驻 | 语义状态表达 |
| 阴影/遮罩 | overlay_medium / overlay_dim | 字幕阴影、调试遮罩、容器压暗 | 背景复杂需提对比 | 成功/错误语义 |

## 4. 同一显示语境“去重”硬规则（强约束）

### 4.1 语义唯一性规则

在同一个显示语境中，一种语义只能映射一个 token；一个 token 不能同时承载两个语义。

1. 交互语义：默认、聚焦、按下、禁用必须是 4 个不同 token。
2. 状态语义：成功、警告、错误必须是 3 个不同 token。
3. 进度语义：悬停进度与处理中进度必须使用不同 token。

### 4.2 语境内排它规则

| 显示语境 | 必须区分的语义 | 禁止的重复 |
|---|---|---|
| 按钮区（左右手势按钮） | 默认/聚焦/按下/禁用/危险 | 用同一 token 同时表示“默认”和“危险” |
| 光标区（指尖圈+读条） | 聚焦/处理中/关闭危险 | 用同一 token 同时表示“处理中”和“关闭危险” |
| 卡片区（标题/正文/分割线） | 强调文本/正文/结构线/状态结果 | 用强调色充当结构线 |
| 状态条+字幕区 | 普通文本/强调状态/警告/错误 | 把错误色用于普通状态字或处理中 |

### 4.3 红色使用规则

1. 红色（status_error）仅允许用于错误、危险、停止。
2. 红色不得出现在默认可点击按钮、处理中进度、聆听状态。

## 5. 改色 AI 的执行流程（必须按顺序）

1. 先标注当前颜色用途：默认/聚焦/按下/禁用/进度/状态/结构。
2. 再按本表替换 token，不改语义层级。
3. 不允许写死新的十六进制色值。
4. 若出现“一个 token 在同语境承载两种语义”，优先保留语义更强的一方，另一方改为对应专用 token。
5. 变更后必须做人眼检查：按钮区、光标区、卡片区、状态条/字幕区四区同时截图核对。

## 6. 快速验收清单（给执行 AI）

| 检查项 | 通过标准 |
|---|---|
| 红色约束 | 仅出现在错误/危险/停止 |
| 交互 4 态 | 默认/聚焦/按下/禁用颜色互不相同 |
| 双进度区分 | 悬停进度 != 处理中进度 |
| 结构色纯净 | 边框/分割线/遮罩未使用语义色 |
| 同语境不复用 | 同一区域内同 token 不承载两种语义 |
| token 纯净性 | 所有改动均来自 design_system.xml 既有 token |

## 7. 参考基线

- 现状问题图：实机图片/拍照追问1.png（红+紫+青绿并存，语义割裂）
- 现状问题图：实机图片/药品识别.png（红+橙+青绿并存，语义割裂）
- 语义色规则来源：UI_Optimization_Plan_Agent_Onboarding_2026-03-12.md
- token 来源：app/src/main/res/values/design_system.xml

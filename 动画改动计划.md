下面是阶段 3 动画改动计划的同步版。该文件现在同时承担 2 个作用：

1. 保留原始实施思路，便于后续继续推进。
2. 记录 2026-03-15 当前已落地状态，避免“文档还是计划，代码已经变了”。

当前状态说明：

- 已落地：状态动效状态机收口、Toast 入退场资源化、Subtitle 淡出互斥清理、部分文字/容器质感统一。
- 已编译验证：`./gradlew :app:assembleDebug` 通过。
- 待实机确认：P1-3 / P1-4 的“体感是否足够明显”。其中 2026-03-15 已做过一轮参数加重。
- 尚未动手：卡片入退场节奏重调。

**动效实现改动清单与当前状态**

1. 状态 pill 动效收口到状态机，不再让资源自己“无限活着”
   状态：已完成

   已落地文件：
   [app/src/main/java/com/narc/arclient/MainActivity.java](app/src/main/java/com/narc/arclient/MainActivity.java)

   已实现内容：
   MainActivity 内新增 `StatusMotion` 状态枚举与统一控制入口。
   Listening / Processing 的循环动效不再在业务分支里零散启动，而是统一由 `setStatusMotion`、`applyStatusMotion`、`clearStatusMotion` 管理。
   停止条件已覆盖：结果返回、异常返回、卡片关闭、页面暂停、状态切回就绪。

   当前口径：
   只有 Listening / Processing 允许循环。
   其他状态进入 `IDLE` 后会清理状态条与图标动画。

2. 状态 pill 呼吸从“图标脉冲”改为“容器轻呼吸”
   状态：已完成，且已做一轮参数加重

   已落地文件：
   [app/src/main/res/anim/pulse.xml](app/src/main/res/anim/pulse.xml)
   [app/src/main/java/com/narc/arclient/MainActivity.java](app/src/main/java/com/narc/arclient/MainActivity.java)

   已实现内容：
   呼吸目标已从状态图标切到状态 pill 容器。
   Listening 和 Processing 都走 pill 呼吸；Processing 额外叠加图标旋转。

   当前参数：
   scale：1.0 → 1.03
   alpha：1.0 → 0.72
   duration：1200ms
   repeat：infinite，仅状态机许可时启动

   备注：
   这一项最初版本过轻，2026-03-15 已加重一档，目的是让实机上能明确看到“状态活着”。

3. Processing 旋转保留，但由代码显式停
   状态：已完成，且已做一轮参数加重

   已落地文件：
   [app/src/main/res/anim/rotate_processing.xml](app/src/main/res/anim/rotate_processing.xml)
   [app/src/main/java/com/narc/arclient/MainActivity.java](app/src/main/java/com/narc/arclient/MainActivity.java)

   已实现内容：
   Processing 旋转已纳入状态机控制。
   退出 Processing 后会立即停掉，不再残留在结果态或空闲态。

   当前参数：
   rotate：0 → 360
   duration：900ms
   repeat：infinite，仅 Processing 启用

   备注：
   此项原本从 1200ms 调到 1250ms 的改动几乎无感，已在 2026-03-15 改成 900ms 一圈，作为当前可见版本。

4. Toast 改为“小幅位移加透明度”，不用纯 alpha
   状态：已完成，且已做一轮参数加重

   已落地文件：
   [app/src/main/java/com/narc/arclient/ui/ArToastView.java](app/src/main/java/com/narc/arclient/ui/ArToastView.java)
   [app/src/main/res/anim/toast_in.xml](app/src/main/res/anim/toast_in.xml)
   [app/src/main/res/anim/toast_out.xml](app/src/main/res/anim/toast_out.xml)

   已实现内容：
   Toast 已从代码内 `ObjectAnimator alpha` 改成资源化的 slide + alpha。
   旧动画与 hide 回调仍保留互斥逻辑，顶替时不会叠播。

   当前参数：
   入场：24dp 上滑回位 + alpha，`anim_duration_normal`
   退场：14dp 下落 + alpha，`anim_duration_normal`

   备注：
   此项最初版本位移偏弱，2026-03-15 已加大位移和时长，以增强实机可感知性。

5. 卡片入退场升级为“凝聚/溶解”过渡
   状态：已完成第二版（凝聚/溶解）

   规划文件：
   [app/src/main/java/com/narc/arclient/ui/CardMaterializeOverlayView.java](app/src/main/java/com/narc/arclient/ui/CardMaterializeOverlayView.java)
   [app/src/main/res/layout/activity_main.xml](app/src/main/res/layout/activity_main.xml)
   [app/src/main/res/anim/card_fade_in.xml](app/src/main/res/anim/card_fade_in.xml)
   [app/src/main/res/anim/card_fade_out.xml](app/src/main/res/anim/card_fade_out.xml)
   [app/src/main/java/com/narc/arclient/MainActivity.java](app/src/main/java/com/narc/arclient/MainActivity.java)

   当前情况：
   已实现“一次性过渡 overlay”方案。
   入场为碎片凝聚，退场为对应溶解，均为一次性短时动画，不引入常驻刷新。
   MainActivity 已接入 show/close 流程，overlay 不可用时回退到原有 card_fade_in/out。

   当前参数：
   overlay 入场时长：约 380ms
   overlay 退场时长：约 340ms
   卡片 XML 动画保留为辅助手段（fallback 或收尾稳定）

   后续目标：
   在 X2 实机上只微调粒度密度、碎片漂移范围和边缘亮度，不改整体架构。

6. Subtitle 不增加循环，只做读感收口
   状态：继续推进，接近完成

   已落地文件：
   [app/src/main/java/com/narc/arclient/ui/SubtitleStreamView.java](app/src/main/java/com/narc/arclient/ui/SubtitleStreamView.java)

   已实现内容：
   保持无循环策略不变。
   已补上最终态淡出回调的取消逻辑，避免新字幕进入时旧淡出延迟误触发。
   已将阴影收轻一档，减轻发虚感。
   Subtitle 阴影已收回样式层，构造函数不再重复硬编码阴影参数。

   尚未做：
   未进一步重调字幕出现 / 消失时长与卡片、Toast 的统一关系。

**质感统一实现改动清单与当前状态**

1. 文字对比统一从 token 下手，少改组件逻辑
   状态：部分完成

   已落地文件：
   [app/src/main/res/values/design_system.xml](app/src/main/res/values/design_system.xml)
   [app/src/main/res/values/styles.xml](app/src/main/res/values/styles.xml)

   已实现内容：
   `text_primary` 已从 `#ECEDE9` 调为 `#F1F4EF`。
   状态文字样式已提升到更稳的主文字层。

   尚未做：
   没有继续拉开 `text_secondary` / `text_tertiary` 的差距，后续如实机仍偏灰再调。

2. 文字阴影只保可读性，不做发光
   状态：部分完成

   已落地文件：
   [app/src/main/java/com/narc/arclient/ui/SubtitleStreamView.java](app/src/main/java/com/narc/arclient/ui/SubtitleStreamView.java)
   [app/src/main/res/values/styles.xml](app/src/main/res/values/styles.xml)

   已实现内容：
   Subtitle 阴影已收轻。
   Card body 与 Toast 已补充轻阴影参数。

   当前原则：
   只做可读性稳固，不做 glow，不做高半径 shadow。

3. 卡片、字幕、状态条的边框透明度回到同一梯度
   状态：继续推进，接近完成

   已落地文件：
   [app/src/main/res/drawable/bg_card_premium.xml](app/src/main/res/drawable/bg_card_premium.xml)
   [app/src/main/res/drawable/bg_subtitle_premium.xml](app/src/main/res/drawable/bg_subtitle_premium.xml)
   [app/src/main/res/drawable/bg_status_bar.xml](app/src/main/res/drawable/bg_status_bar.xml)
   [app/src/main/res/values/design_system.xml](app/src/main/res/values/design_system.xml)

   已实现内容：
   新增 `border_luminous` 作为卡片微高光描边。
   Subtitle / Status bar 已统一使用 `border_default`。
   Divider token 已略增强。
   其中 Status bar 已进一步回调到 `border_subtle`，让层级关系变为“卡片最强、字幕次之、状态最弱”。

   尚未做：
   还没有基于实机再回调最终平衡。

4. 微发光只给结果卡片，不给字幕和状态条
   状态：已完成第一版

   已落地文件：
   [app/src/main/res/drawable/bg_card_premium.xml](app/src/main/res/drawable/bg_card_premium.xml)

   已实现内容：
   卡片增加了极轻的高光描边层。
   Subtitle 和 Status bar 未加入额外外发光。

   备注：
   当前版本强度仍偏克制，后续是否再上调只看 X2 实机效果。

5. 分割线改单一 token，不再靠局部视觉猜测
   状态：已完成第一版

   已落地文件：
   [app/src/main/res/values/design_system.xml](app/src/main/res/values/design_system.xml)

   已实现内容：
   `divider_line` 已从 15% 调到 18%。
   item_ar_card 的分割线仍继续吃统一 token，没有引入新局部颜色。

6. Subtitle 组件的代码色值覆盖收回到设计系统口径
   状态：继续推进，接近完成

   已落地文件：
   [app/src/main/java/com/narc/arclient/ui/SubtitleStreamView.java](app/src/main/java/com/narc/arclient/ui/SubtitleStreamView.java)

   已实现内容：
   代码层仍保留“实时中 / 最终态”两档切换。
   阴影与淡出互斥已经收口，不再继续扩散到更多状态。
   默认阴影参数已回到样式层，代码不再重复设置。

   尚未做：
   style 与代码之间的职责边界还可以再进一步压实，目前属于“可用但未完全收口”。

**不建议改动的点**

1. 不新增新的动画时长 token。现有 fast、normal、slow 足够。
2. 不给 Subtitle、Toast、Card 同时上持续循环。循环只留给状态 pill 和处理中图标。
3. 不把动效放进 RenderProcessor 的 Canvas 刷新链路。阶段 3 动效仍优先放在 View 层完成。

**后续执行顺序建议（从当前状态继续）**

1. 先在 X2 上复核 P1-3 / P1-4 是否终于可感知。
2. 再收卡片入退场节奏，使卡片、Toast、状态 pill 三者形成稳定层级。

---

## 模式切换按钮图标 & 动画改造（2026-03-16）✅ 已完成

### 一、图标替换

状态：**已完成**

**落地文件：**

- [app/src/main/res/drawable/ic_mode_photo.png](app/src/main/res/drawable/ic_mode_photo.png) — 拍照识药图标（96×96 白色透明 PNG）
- [app/src/main/res/drawable/ic_mode_subtitle.png](app/src/main/res/drawable/ic_mode_subtitle.png) — 实时字幕图标（96×96 白色透明 PNG）
- [app/src/main/res/drawable/ic_mode_multi.png](app/src/main/res/drawable/ic_mode_multi.png) — 拍照追问图标（96×96 白色透明 PNG）
- [app/src/main/java/com/narc/arclient/process/processor/RenderProcessor.java](app/src/main/java/com/narc/arclient/process/processor/RenderProcessor.java)

**已实现内容：**

原按钮用 `canvas.drawText("CC"/"📷"/"🎤")` 绘制，已替换为：

1. 构造器中通过 `BitmapFactory.decodeResource` 加载 3 张图标到 `modeIconBitmaps[3]`
2. 绘制时用 `canvas.drawBitmap(icon, null, dst, iconPaint)` 替代文字
3. 背景圆三种颜色（橙 `#FF9500` / 青 `#00C7BE` / 紫 `#AF52DE`）保持不变

**图标素材来源：**

原始素材来自 `UI素材/` 文件夹（白底整图），通过 PIL 脚本自动提取深色前景像素（亮度阈值 120），裁剪 bbox + 18% padding 后缩放至 96×96 透明 PNG。

---

### 二、切换动画（A 弹跳缩放 + C 颜色波纹）

状态：**已完成**

**落地文件：**

- [app/src/main/java/com/narc/arclient/process/processor/RenderProcessor.java](app/src/main/java/com/narc/arclient/process/processor/RenderProcessor.java)

**已实现内容：**

**A. 图标弹跳缩放（Icon Bounce）**

切换瞬间图标从正常大小弹出，到 1.18× 后回弹到 1.0×。
曲线公式：`scale = 1 + 0.18 × sin(t × π)`，`t ∈ [0, 1]`

**C. 颜色波纹扩散（Ripple）**

切换瞬间从按钮中心向外扩散一个带「旧模式颜色」的圆环，半径从 `btnRadius` 扩散到 `btnRadius × 2.6`，透明度从 200 衰减到 0。
旧颜色作为波纹色，给人"从旧模式离开"的直觉感。

**参数调整位置（RenderProcessor.java 字段区）：**

```java
private static final long ICON_BOUNCE_MS = 300;   // 弹跳动画总时长（毫秒），越小越"脆"
private static final long RIPPLE_MS     = 400;   // 波纹扩散总时长（毫秒），越大越慢
```

其他可调项（在绘制段 drawEye 方法内）：

| 参数 | 当前值 | 说明 |
|---|---|---|
| 弹跳峰值倍数 | `0.18f` | `scale = 1 + 0.18 × sin(...)` 内的系数，越大弹跳越明显 |
| 波纹终止半径倍数 | `1.6f` | `subtitleBtnRadius × 1.6f`，越大波纹扩散越远 |
| 波纹初始透明度 | `200` | `int rippleAlpha = 200 × (1 - t)`，越大越明显 |
| 波纹描边宽度 | `4f` | `ripplePaint.setStrokeWidth(4f)` |

---

### 三、模式切换演示流程（本地模拟）

状态：**已完成**

**落地文件：**

- [app/src/main/java/com/narc/arclient/MainActivity.java](app/src/main/java/com/narc/arclient/MainActivity.java)

**已实现内容：**

App 启动后自动触发一次模式切换演示，顺序为 `实时字幕 → 拍照追问 → 拍照识药 → （循环）`，每次切换触发完整的 Toast、ArToast、状态更新和 Canvas 动画。

演示在本地 UI 模拟（处理中卡片）结束后启动，避免冲突。

**参数调整位置（MainActivity.java 字段区）：**

```java
private static final boolean ENABLE_MODE_SWITCH_DEMO = true;  // false 可关闭演示
private static final long    MODE_DEMO_INTERVAL_MS   = 2000L; // 每次切换间隔（毫秒）
private static final int     MODE_DEMO_CYCLES        = 2;     // 循环圈数，完成后自动停止
```

演示启动延迟 = `LOCAL_UI_SIM_START_DELAY_MS (1200ms) + LOCAL_UI_SIM_RESULT_DELAY_MS (2600ms) + 1000ms = 约 4.8s`
3. 最后根据实机图回调卡片微发光和边框强度，不盲调。

---

## 参数调整途径（2026-03-15 可执行版）

目的：后续调参时，直接按“入口文件 + 参数名 + 推荐范围 + 观察点”执行，避免盲改。

### A. 卡片凝聚/溶解（主视觉）

- 参数入口：
   [app/src/main/java/com/narc/arclient/ui/CardMaterializeOverlayView.java](app/src/main/java/com/narc/arclient/ui/CardMaterializeOverlayView.java)

- 核心参数与推荐范围：
   - `MATERIALIZE_DURATION_MS`：320–460ms
   - `DISSOLVE_DURATION_MS`：280–420ms
   - `GRID_COLS / GRID_ROWS`：14x10 到 20x14
   - `PARTICLE_COUNT`：20–40
   - 碎片漂移（`travel`）：入场 10–16dp，退场 12–20dp
   - 粒子半径（`radius`）：1.0–3.0px
   - 粒子 alpha（`particlePaint`）：120–200

- 调参顺序：
   1. 先调时长（先定节奏）
   2. 再调碎片密度（再定“颗粒感”）
   3. 最后调亮度（避免抢正文）

- 实机观察点：
   - 入场是否“先成轮廓后成内容”
   - 退场是否“干净收走，不拖泥带水”
   - 动效是否压过正文可读性

### B. 状态 pill 呼吸 + Processing 旋转（状态动效）

- 参数入口：
   [app/src/main/res/anim/pulse.xml](app/src/main/res/anim/pulse.xml)
   [app/src/main/res/anim/rotate_processing.xml](app/src/main/res/anim/rotate_processing.xml)
   [app/src/main/java/com/narc/arclient/MainActivity.java](app/src/main/java/com/narc/arclient/MainActivity.java)

- 核心参数与推荐范围：
   - `pulse` scale：1.01–1.05
   - `pulse` alpha 低值：0.68–0.82
   - `pulse` duration：900–1500ms
   - `rotate_processing` duration：800–1200ms/圈

- 护栏：
   - 循环仅 Listening / Processing 允许
   - 停止条件必须走 `setStatusMotion(StatusMotion.IDLE)`
   - 不允许在业务分支里直接常驻 `startAnimation`

### C. Toast 入退场（轻提示层）

- 参数入口：
   [app/src/main/res/anim/toast_in.xml](app/src/main/res/anim/toast_in.xml)
   [app/src/main/res/anim/toast_out.xml](app/src/main/res/anim/toast_out.xml)
   [app/src/main/java/com/narc/arclient/ui/ArToastView.java](app/src/main/java/com/narc/arclient/ui/ArToastView.java)

- 核心参数与推荐范围：
   - 入场位移：16–28dp
   - 退场位移：8–18dp
   - alpha 时长：`anim_duration_fast` 或 `anim_duration_normal`
   - 显示停留：`SHOW_DURATION_MS` 建议 1600–2600ms

- 实机观察点：
   - 有“滑入滑出”感但不抢卡片
   - 连续 Toast 顶替无叠播、无闪烁

### D. 卡片基础动画（fallback / 收尾稳定）

- 参数入口：
   [app/src/main/res/anim/card_fade_in.xml](app/src/main/res/anim/card_fade_in.xml)
   [app/src/main/res/anim/card_fade_out.xml](app/src/main/res/anim/card_fade_out.xml)

- 核心参数与推荐范围：
   - 入场 scale 起点：0.95–0.98
   - 入场位移：10–18dp
   - 退场 scale 终点：0.96–0.99
   - 退场位移：-8 到 -14dp

- 原则：
   - 它现在是“辅助手段”，不与 overlay 主视觉争戏

### E. 字幕出现/消失（读感层）

- 参数入口：
   [app/src/main/java/com/narc/arclient/ui/SubtitleStreamView.java](app/src/main/java/com/narc/arclient/ui/SubtitleStreamView.java)
   [app/src/main/res/values/styles.xml](app/src/main/res/values/styles.xml)

- 核心参数与推荐范围：
   - `TYPE_INTERVAL_MS`：28–45ms
   - `MAX_TYPE_DURATION_MS`：280–500ms
   - `FINAL_STAY_MS`：1800–2600ms
   - `fadeDurationMs`：`anim_duration_normal` 优先

---

## 动画交互统一化提案（下一步）

目标：把“卡片、状态、Toast、字幕”的节奏与触发语义统一成一套系统，减少后续维护成本。

### 1. 建立统一的 Motion 语义层

- 建议新增统一语义（文档先行，代码后续）：
   - `enter_emphasis`：卡片凝聚/关键内容入场
   - `enter_soft`：Toast/字幕入场
   - `loop_status`：Listening/Processing 的状态循环
   - `exit_soft`：Toast/字幕消失
   - `exit_emphasis`：卡片溶解退场

- 价值：
   - 后续改时长时，不需要在多个文件“凭感觉”改数值

### 2. 统一时长映射关系

- 建议关系：
   - `enter_emphasis` ≈ `anim_duration_slow`
   - `enter_soft` / `exit_soft` ≈ `anim_duration_normal`
   - `loop_status` 周期取 `normal` 的 4–6 倍
   - `exit_emphasis` 不快于 `enter_soft`

- 结果：
   - 用户体感会形成稳定预期：卡片最稳，Toast 次之，状态最轻

### 3. 统一触发与停止责任边界

- 当前建议（继续保持）：
   - `MainActivity` 负责状态切换与动效启停
   - 组件类只负责自己的播放细节，不自行决定长期循环

- 后续可升级：
   - 抽一个 `MotionCoordinator`（或同等职责类），把 `setStatusMotion` 与卡片/Toast 触发点集中管理

### 4. 统一“可拍验收脚本”

- 每次调参固定拍 4 组证据：
   1. Listening 3–5 秒
   2. Processing 3–5 秒
   3. 卡片入场 + 退场各 1 段
   4. Toast 连续出现 2 次

- 每组验收只看三件事：
   - 可感知
   - 不抢戏
   - 可停止

### 5. 与渲染层的统一策略（重要）

- 现状：按钮交互仍有一部分在 `RenderProcessor` 侧表达。
- 建议：
   - 保持“View 层负责信息过渡动效，Render 层只做必要交互反馈”的边界
   - 不把当前卡片凝聚/溶解迁移到 Render 主链

这样可避免为追求一致性而引入常驻刷新风险。
